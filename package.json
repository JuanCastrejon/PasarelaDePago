{
  "name": "pasarela-de-pago",
  "version": "0.1.0",
  "private": true,
  "description": "Base fundacional de una plataforma de payment orchestration para Colombia.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^24.9.0",
    "typescript": "^5.9.3"
  }
}
