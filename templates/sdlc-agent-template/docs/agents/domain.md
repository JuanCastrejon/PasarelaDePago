# Domain Docs

## Layout

El repositorio usa layout `single-context`.

## Fuente principal

- `CONTEXT.md`
- `indice-operativo.md`

## Documentacion complementaria

- `docs/domain/`
- `docs/adr/`
- `docs/requirements/`
- `docs/backlog/`
- `openspec/specs/`

## Regla de consumo

1. Leer `CONTEXT.md` para vocabulario base.
2. Leer `indice-operativo.md` para flujo y validaciones.
3. Leer `docs/domain/` para reglas e invariantes.
4. Leer `docs/adr/` antes de proponer cambios estructurales.
5. Leer `docs/requirements/`, `docs/backlog/` y `openspec/specs/` para trabajo de producto y ejecucion.
